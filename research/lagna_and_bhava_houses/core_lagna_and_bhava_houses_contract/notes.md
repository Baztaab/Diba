# non-binding

- Which house paths depend on `houses_ex(...)[0]` cusp arrays and which consume `ascmc` arrays?
- How is Lagna indexing validated when `planet_positions[0]` is assumed at house assignment boundaries?
- Which house-system branches use numeric vs letter house codes before SwissEph invocation?
