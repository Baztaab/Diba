# non-binding

- How is global sidereal mode coordination tracked when multiple modules set mode in sequence?
- Which call paths rely on one-argument mode switching versus explicit `(mode, value, jd)` forms?
- Which callsites treat `jd` and `jd_utc` as the third argument basis?
