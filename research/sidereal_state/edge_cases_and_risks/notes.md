# non-binding

- Which functions define the reset boundary when `set_ayanamsa_mode(...)` appears in helper layers?
- How is mode state isolated when house functions return directly from `swe.houses_ex(...)`?
- Which nested helpers depend on outer reset behavior rather than local reset behavior?
