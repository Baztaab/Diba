# non-binding

- Which call paths use `jd_utc` in `swe.calc_ut` and which pass `jd` directly?
- How are rounding factors chosen for each element of the SwissEph output vector?
- How is Ketu handling traced where values are derived from Rahu paths?
