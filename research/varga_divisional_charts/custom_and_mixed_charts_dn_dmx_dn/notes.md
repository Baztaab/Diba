# non-binding

- Which boundaries switch between standard function dispatch and custom Dn dispatch?
- Which combinations of `varga_factor_1` and `varga_factor_2` change composition order behavior?
- Which non-cyclic option sets are consumed for start-sign variation and end-of-sign counting?
