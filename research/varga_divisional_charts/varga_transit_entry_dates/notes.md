# non-binding

- Which increment-step choices dominate search runtime in divisional and mixed entry-date paths?
- Which conjunction paths use inverse-lagrange tuning and which paths fall back to alternate tuning?
- Which planet identifiers invoke branch-specific handling in divisional entry search?
