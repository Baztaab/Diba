# non-binding

- Which boundaries expose standard Dn routing versus custom Dn routing for the same factor?
- Which helper outputs are consumed directly by standard chart methods and which are transformed first?
- Which call paths depend on global sidereal mode state during varga computation?
