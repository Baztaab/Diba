# non-binding

- What behavior is observed for DST fold or gap local times when deriving `jd_utc` from civil inputs?
- What runtime effect appears when `local_time_to_jdut1()` passes `0` in `swe.utc_to_jd(..., 0, ...)`?
- How are assumed `dob` and `tob` values traced when `_validate_data()` receives missing inputs?
