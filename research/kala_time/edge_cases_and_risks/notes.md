# non-binding

- Which calendar paths depend on assumed `dob` or `tob` values when caller inputs are partial?
- Which calculations consume the recomputed `rise_jd` rather than the direct SwissEph output in `sunrise()`?
